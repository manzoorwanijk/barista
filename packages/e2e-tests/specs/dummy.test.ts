/// <reference types="jest-playwright-preset" />
/// <reference types="expect-playwright" />

describe('DummyTest', () => {
	it('Does nothing more than activating the plugin', async () => {
		expect(true).toBe(true);
	});
});
